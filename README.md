# LFS-Pain
This project aims to build a minimal Linux system from scratch. The goal is to compile and configure the toolchain, kernel, core utilities, and bootloader to create a fully bootable Linux environment.
